<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task</title>
</head>
<body>
    <p><a href="die.php"></a> Create a custom error handling using die() function when unable to open  certain file</p>
    <p><a href="handler.php"> Create a custom error handler using set error handler that shows error no and error string</a></p>
    <p>Trigger an error for certain variable value greater than 100</p>
    <p>Use E_USER_WARNING if a variable is greater than 1 and trigger a custom error</p>
    <p>Use E_USER_NOTICE if a file exist else use E_USER_ERROR and trigger error</p>
    <p>Design a navbar and show custom error instead of error 404</p>
    <p>Log errors into my-error.log file</p>
    <p>Create a custom error displaying the file name and line number of the error</p>
    <p>Use error_get_last() function print message from the last error that occurred</p>
    <p>Send error message to the server log if error connecting to the database</p>
</body>
</html>